/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}', // Watch all files in the src folder
  ],
  theme: {
    extend: {
      // We can add custom colors, fonts, etc. here later
    },
  },
  plugins: [],
};